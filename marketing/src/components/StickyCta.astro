---
import {
  cohortStartDateCompact,
  doorsCloseDate,
  enrollUrl,
  isWaitlist,
  offerPrice,
  quizUrl,
  waitlistPrice,
  weeklyPrice,
} from '../config';
---

<div class="sticky-cta" id="stickyCta" aria-hidden="true">
  {
    isWaitlist ? (
      <>
        <div class="s-price">
          <strong>Full ${offerPrice} · unlock ${waitlistPrice} in the quiz</strong>
          Doors close {doorsCloseDate} · about ${weeklyPrice}/week early
        </div>
        <a class="btn" href={quizUrl}>
          Take the quiz
        </a>
      </>
    ) : (
      <>
        <div class="s-price">
          <strong>${offerPrice} for 8 weeks · starts {cohortStartDateCompact}</strong>
          About ${weeklyPrice}/week · doors close {doorsCloseDate}
        </div>
        <a class="btn" href={enrollUrl}>
          Join now
        </a>
      </>
    )
  }
</div>
