---
import {
  cohortStartDateCompact,
  doorsCloseDate,
  enrollUrl,
  fullPrice,
  isWaitlist,
  offerPrice,
  quizUrl,
  waitlistPrice,
} from '../config';
---

<div class="sticky-cta" id="stickyCta" aria-hidden="true">
  {
    isWaitlist ? (
      <>
        <div class="s-price">
          <strong>Free ranges</strong>Lock ${waitlistPrice} · full ${fullPrice}
        </div>
        <a class="btn" href={quizUrl}>
          See your ranges
        </a>
      </>
    ) : (
      <>
        <div class="s-price">
          <strong>${offerPrice} · starts {cohortStartDateCompact}</strong>
          Enrollment open · doors close {doorsCloseDate}
        </div>
        <a class="btn" href={enrollUrl}>
          Join now
        </a>
      </>
    )
  }
</div>
