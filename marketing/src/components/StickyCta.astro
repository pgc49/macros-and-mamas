---
import {
  cohortStartDateCompact,
  doorsCloseDate,
  enrollUrl,
  isWaitlist,
  offerPrice,
  quizUrl,
  waitlistPrice,
  weeklyPrice,
} from '../config';
---

<div class="sticky-cta" id="stickyCta" aria-hidden="true">
  {
    isWaitlist ? (
      <>
        <div class="s-price">
          <strong>${waitlistPrice} for 8 weeks</strong>
          About ${weeklyPrice}/week · free ranges while you wait
        </div>
        <a class="btn" href={quizUrl}>
          Get your ranges
        </a>
      </>
    ) : (
      <>
        <div class="s-price">
          <strong>${offerPrice} for 8 weeks · starts {cohortStartDateCompact}</strong>
          About ${weeklyPrice}/week · doors close {doorsCloseDate}
        </div>
        <a class="btn" href={enrollUrl}>
          Join now
        </a>
      </>
    )
  }
</div>
