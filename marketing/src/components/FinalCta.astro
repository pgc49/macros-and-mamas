---
import {
  cohortStartDate,
  doorsCloseDate,
  enrollUrl,
  isWaitlist,
  offerPrice,
  quizUrl,
  waitlistPrice,
  weeklyPrice,
} from '../config';
---

<section class="final">
  <div class="wrap">
    <span class="script">Your season. Your ranges. Your coach.</span>
    <h2>Feel like yourself again.</h2>
    {
      isWaitlist ? (
        <div class="open-cta" style="position:relative;z-index:2">
          <p class="lede">
            Next cohort opening soon. Get your free macro-range preview and be
            first when doors open. The 8-week program is ${waitlistPrice}
            (about ${weeklyPrice}/week) — no charge to join the list.
          </p>
          <a
            class="btn"
            href={quizUrl}
            style="background:#fff;color:var(--accent-deep)"
          >
            Get your macro ranges
          </a>
        </div>
      ) : (
        <div class="open-cta" style="position:relative;z-index:2">
          <a
            class="btn"
            href={enrollUrl}
            style="background:#fff;color:var(--accent-deep)"
          >
            Join now · starts {cohortStartDate}
          </a>
          <p class="price-note">
            ${offerPrice} · doors close {doorsCloseDate} or when the cohort fills
          </p>
          <p class="price-note" style="margin-top:10px">
            <a href={quizUrl} style="color:inherit;text-decoration:underline"
              >Or get your macro ranges first</a
            >
          </p>
        </div>
      )
    }
  </div>
</section>
